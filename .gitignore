# Claude Code local config
.claude
CLAUDE.md

# Graphify knowledge graph — local-only, regenerated from source.
# Nothing here belongs on GitHub; the graph is rebuilt from code on demand.
.graphifyignore
.graphify/
graph-watch.cmd
refresh-graph.cmd
graph.html

# Generated by `graphify hook install` (merge driver for .graphify/graph.json,
# which is itself ignored). Remove this line if you ever add real attributes.
.gitattributes
